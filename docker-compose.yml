services:
  db:
    image: postgres:16
    container_name: recsys_db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: recsys_db
    ports:
      - "5432:5432"