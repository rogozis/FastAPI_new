version: '3.13'
services:
  db:
    image: postgres:15
    container_name: recsys_db
    environment:
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: mypassword
      POSTGRES_DB: recsys_db
    ports:
      - "5432:5432"